{
  "routes": [
    {
      "method": "GET",
      "path": "/series",
      "handler": "Series.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/series/count",
      "handler": "Series.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/series/:_id",
      "handler": "Series.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/series",
      "handler": "Series.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/series/:_id",
      "handler": "Series.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/series/:_id",
      "handler": "Series.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
